// use std::env;
// use std::fs;

mod cpu;
mod opcodes;

#[macro_use]
extern crate bitflags;

// use cpu::CPU;

fn main() {
    // let data: Vec<u8> = vec![0x85, 0x10, 0x00];

    // let mut cpu = CPU::new();

    // cpu.load(data);
    // cpu.reset();
    // cpu.reg_a = 0x55;
    // cpu.run();

    // println!("{}", cpu.mem_read(0x10));
    println!("{}", "Hello, World!");
}